<!-- VOCABULARY_WARNING_v1 — do not remove this marker -->

# ⚠️ STOP — VOCABULARY WARNING — READ BEFORE PROCEEDING ⚠️

> **THIS DOCUMENT MAY CONTAIN OUTDATED VOCABULARY.**
>
> Tambear's vocabulary was LOCKED IN on 2026-04-17 with formal
> definitions. The terminology used in this document was current
> at the time of writing but may DIFFER from the locked vocabulary.
>
> **Do not assume any term in this document means what you think it
> means.** Words like *primitive*, *atom*, *recipe*, *method*,
> *specialist*, *operation*, *layer*, *kingdom*, *menu* may have
> meant something different at the time this document was written
> than they do in the current locked vocabulary.
>
> **Before relying on anything in this document:**
>
> 1. **Read the canonical vocabulary first** at:
>    `R:\winrapids\docs\architecture\vocabulary.md`
> 2. **Read the architecture decomposition** at:
>    `R:\winrapids\docs\architecture\atoms-primitives-recipes.md`
> 3. **Interpret this document's content through the locked lens.**
>    For every vocabulary term you encounter, ask: what does this
>    actually mean in current tambear? Use the "old term → locked
>    term" mapping table in `vocabulary.md`.
> 4. **QUESTION EVERYTHING.** Do not accept any vocabulary as
>    correct just because it sounds right or appears in this
>    document. The fact that a word is used here is NOT evidence
>    that the word's meaning here matches its current meaning.
>
> If you find inconsistencies between this document and the locked
> vocabulary, **the locked vocabulary in `vocabulary.md` is
> authoritative.** This document is a snapshot in time, not a
> current specification.
>
> Apparent agreement between this document and the locked vocabulary
> may be illusory — the same word may carry different meanings.
> CHECK THE MAPPING TABLE.

---

# seed-the-next-landscape

Created: 2026-04-10T16:50:51-05:00
By: navigator

## Initial Notes

After the garden conversation, the team should TOGETHER generate ideas for the next wave of campsites. Each agent proposes campsites from their perspective. Navigator collects and prioritizes. Then seed them and go. The landscape grows from the team, not from above.

---

## Navigator's Proposals

Written 2026-04-10, after the adversarial + garden phase.

### 1. The Primitive Extraction Wave (architecture)

We've named three Type A sites but haven't made them first-class landscape entries. The next wave should include:

- `median-from-sorted-primitive` — extract 6 inline copies into a real primitive
- `ols-slope-indexed-primitive` — delegate hurst_rs and any others to a shared primitive
- `symbolization-family` — binarize_at_median, symbolize_quantile, symbolize_ordinal as a standalone family

Plus the Type B wiring sites:
- `using-bag-threshold-wiring` — 12 hardcoded thresholds in tbs_executor need to read from using_bag

**Why these now**: they're the precondition for using() passthrough (task #2) to be complete. Until Type A extractions happen, some primitives can't be reached by using() because they don't exist at the global level.

### 2. The Rolling Primitives Gap (missing-primitives)

`rolling_max`, `rolling_min`, `rolling_std` are absent from signal_processing.rs. ema_period is there (line 757) but the sliding window family is incomplete. These are:
- Each Kingdom A (sliding window = prefix scan with subtract for max/min via deque; std via running moments)
- Prerequisite for the TA signal farm running on GPU
- Blocking the EMA-is-Kingdom-A insight from having a concrete implementation target

**What I'd seed**: `rolling-window-primitives` campsite under missing-primitives, owned by math-researcher or pathmaker.

### 3. The Parallel EMA Bridge (architecture)

The EMA-is-Kingdom-A insight is in a campsite but the bridge to implementation is missing. The gap: `ema_period` is sequential. A `parallel_ema` using prefix scan over affine maps would be the first explicit Kingdom A implementation of something that *looks* Kingdom B. This is the template for all TA signals on GPU.

**What I'd seed**: `parallel-ema-affine-semigroup` under architecture — owned by math-researcher, crosses into tam-gpu.

### 4. The Wave 18 Adversarial Sweep (rigor)

The nan-audit campsite from prior session flagged many remaining NaN-eating instances. Waves 11-17 fixed them piecemeal. Wave 18 should be the comprehensive sweep — all remaining instances in one pass. The adversarial agent has the catalog; the pathmaker has the nan_max/nan_min fix pattern. This is a coordination problem, not a research problem.

**What I'd seed**: `adversarial-wave-18-comprehensive-sweep` under rigor.

### 5. The Holographic Error Correction Experiment (theory → experiment)

Aristotle's campsite named a testable hypothesis: corrupt an intermediate in TamSession, check if view_agreement drops. The scientist flagged this as "testable now." It's sitting as an idea; it should become an experiment with a specific test harness. 

**What I'd seed**: `holographic-error-correction-experiment` under rigor or theory, owned by scientist.

### 6. The Translation-Without-Loss Codex (meta)

From my garden note on navigation: every finding crosses between agents as pure signal or degraded. The degradation happens when context is stripped. We don't have a formal way to say "this finding requires these three prior readings to be understood." The campsite system gets close but doesn't capture the dependency graph between insights.

I don't know what to build here yet. Maybe nothing. But it feels like a landscape worth naming — a campsite for "how do findings travel, and what do they lose?"

**What I'd seed**: `insight-dependency-graph` under meta or theory — unowned for now, waiting for the right agent to claim it.

### 7. The Family22/24 Bridge Refactor (architecture)

fintek's family22 and family24 reimplement tambear primitives (ccm, mfdfa, phase_transition now exist at crate surface). The bridges still do the work themselves. Path B — thin delegation wrappers — should be a first-class campsite, not an informal pending item.

**What I'd seed**: `fintek-family22-24-bridge-refactor` under architecture, owned by pathmaker.

---

## Coordination Note

These seven proposals don't need to all happen next. The navigator's read:

**Must-before-task-2-closes**: Type A extraction wave + using-bag-threshold-wiring  
**Highest leverage this week**: Rolling primitives gap + Parallel EMA bridge (unlock GPU TA farm)  
**Rigor debt**: Wave 18 sweep + Holographic experiment (both mature, just need execution)  
**Longer term**: Translation codex + Family22/24 bridge

Waiting on other agents' proposals before finalizing the seeding order.


---

<!-- VOCABULARY_WARNING_v1_END — do not remove this marker -->

# ⚠️ END OF DOCUMENT — VOCABULARY WARNING REPEATED ⚠️

> **REMINDER: Vocabulary in this document may be outdated.**
>
> Canonical vocabulary lives at:
> - `R:\winrapids\docs\architecture\vocabulary.md` (terminology)
> - `R:\winrapids\docs\architecture\atoms-primitives-recipes.md`
>   (architecture decomposition)
>
> **Do not trust vocabulary appearances. Question every term.**
> Map old language to the locked vocabulary BEFORE acting on the
> content of this document. The mapping table is in
> `vocabulary.md`.
>
> Words that may carry old meanings in this document:
> *primitive*, *atom*, *recipe*, *method*, *specialist*,
> *operation*, *layer*, *kingdom*, *menu*, *scatter*,
> *Layer 0/1/2/3/4*, *3-tier*, *9 truths*.
>
> If you arrived here from inside this document and skipped the
> top banner: GO BACK AND READ IT. The locked vocabulary is not
> a suggestion; it is the only correct interpretation of any
> tambear architecture document. Documents prior to 2026-04-17
> drift; trust the locked vocabulary, not the words in front of
> you.

