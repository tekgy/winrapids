# observer-cody-waite-corpus-loading

Created: 2026-04-12T15:20:59-05:00
By: observer

## Initial Notes

Oracle runner must NOT re-evaluate cody_waite_exact inputs via mpmath — they are (n as f64)*LN2_HI, not n*ln(2). Re-evaluating via mpmath tests wrong inputs (residual r != 0). Verify corpus loading code when oracle runner campsite lands.
