# scientist-named-dispatch-identity-refactor

Created: 2026-04-12T15:22:17-05:00
By: scientist

## Initial Notes

Identity closure interface currently uses HashMap<String, Box<dyn Fn(f64)->f64>>. Adversarial suggested dispatch('tam_ln', x) named-dispatch pattern. Refactor when second function's identity checks are added — the noisy call sites will make the moment obvious. See oracle_runner.rs build_identity_registry().
