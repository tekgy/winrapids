# scientist-identity-check-two-argument

Created: 2026-04-12T15:23:23-05:00
By: scientist

## Initial Notes

exp_additivity identity check uses a proxy: a=x/2, b=x/2, then checks exp(a)*exp(b) vs exp(a+b). A real two-argument sweep needs a different interface (two inputs from domain^2 filtered by a+b in-range). Current proxy finds the same class of bugs but with lower diversity. Phase 2: proper two-argument identity sweep interface in runner.
