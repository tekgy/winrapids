# observer-gpu-sum-tolerance-root-cause

Created: 2026-04-12T15:24:57-05:00
By: observer

## Initial Notes

gpu_sum_matches_cpu uses 1e-6 absolute tolerance for sum of 1..10000 = 50005000. CPU result is exact. 1e-6 is ~18 ULP of the result. Never root-caused: is this actual atomicAdd non-determinism on this dataset, or was it conservative guessing? Should measure empirically before Peak 6 replaces it with bit-exact.
