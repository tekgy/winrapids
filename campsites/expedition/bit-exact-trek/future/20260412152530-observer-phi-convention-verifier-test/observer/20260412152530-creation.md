# observer-phi-convention-verifier-test

Created: 2026-04-12T15:25:30-05:00
By: observer

## Initial Notes

From Entry 001: IR spec phi-suffix convention (%acc/%acc') makes SSA invariant harder to enforce. Verifier (campsite 1.9) should catch: (a) %acc' defined but %acc not, (b) %acc modified outside loop body. When verifier landed I noted it passes the variance_pass test — but never specifically tested these two phi edge cases. Check if verifier test suite covers them.
