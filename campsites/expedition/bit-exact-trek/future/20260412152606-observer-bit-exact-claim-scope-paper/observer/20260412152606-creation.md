# observer-bit-exact-claim-scope-paper

Created: 2026-04-12T15:26:06-05:00
By: observer

## Initial Notes

Publishability: the claim 'cpu.to_bits() == cuda.to_bits() == vulkan.to_bits()' is novel if demonstrated on a non-trivial kernel (variance, not just addition). Draft the peer-review Q&A: Q1=portability across GPU models, Q2=not a codegen coincidence (show PTX differs from NVRTC PTX but bits agree), Q3=libm ULP backing, Q4=subnormal/NaN/inf behavior. Each question needs a specific test.
