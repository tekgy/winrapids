# observer-proptest-nan-roundtrip

Created: 2026-04-12T15:26:30-05:00
By: observer

## Initial Notes

tambear-tam-ir proptest generates 10k random programs. Does it generate programs with NaN constants? If the generator uses f64::arbitrary() from proptest, NaN inputs are possible. But the NaN PartialEq issue means two ASTs with NaN constants can't be compared with ==. Check whether proptest::roundtrip_10000_random_programs can ever generate NaN constants and whether the test handles them correctly.
