# adversarial-oracle-toml-schema-v2

Created: 2026-04-12T15:28:51-05:00
By: adversarial

## Initial Notes

The three-section oracle TOML schema is stable for Phase 1. Before Phase 2, consider schema v2: (1) add 'domain_exclusions' field for functions like tan (exclude |cos(x)| < 2^-26 pole neighborhoods), (2) add 'accuracy_class' field per corpus set (some sets are ULP-checked, some are constraint-only, some are both), (3) add 'requires_auxiliary_functions' field to identity_checks so the runner can warn explicitly when a dependency is missing rather than silently skipping. Draft schema v2 before the first Phase 2 oracle TOML is written.
