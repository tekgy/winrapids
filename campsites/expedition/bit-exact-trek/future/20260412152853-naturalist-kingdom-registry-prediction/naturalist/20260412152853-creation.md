# naturalist-kingdom-registry-prediction

Created: 2026-04-12T15:28:53-05:00
By: naturalist

## Initial Notes

The synthesis.md predicted a fourth registry (device capabilities, naturalist agreed). The synthesis also named kingdoms and ops as additional candidates. Prediction: a KINGDOM registry will emerge when Peak 2 starts wanting Kingdom B primitives (AR/ARIMA, Affine(A,b) scan) that don't fit Kingdom A accumulate+gather. Trigger: first non-commutative-monoid reduction that pathmaker tries to express in .tam IR. Next session should watch for the moment the existing scalar phi + grouping mechanism hits its ceiling for sequential dependencies, because that's when the registry slot will crystallize around its first kingdom-B entry.
