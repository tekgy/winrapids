# aristotle-fusion-compatibility-theorem

Created: 2026-04-12T15:31:38-05:00
By: aristotle

## Initial Notes

Formally prove SequentialLeft ↔ TreeFixedFanout(k) fusability under specific conditions. I designed Option D (is_fusable_with predicate) in Phase 6 deep dive of accumulate-gather-phases.md but nobody has actually proven any non-trivial pair is bit-exactly fusable. The theorem obligation lives in campsite 1.15 per navigator's earlier routing, but the proof sketch is also aristotle-natural work. Start point: for a fused loop where loop index runs 0..N, can acc1 (SequentialLeft) and acc2 (TreeFixedFanout(4)) each produce bit-exactly the same output as running in isolation? Probably yes iff TreeFixedFanout(4) is interpreted as 'group 4 consecutive values in loop order then tree-reduce' rather than 'arbitrary grouping'. Pin the definition, prove the theorem.
