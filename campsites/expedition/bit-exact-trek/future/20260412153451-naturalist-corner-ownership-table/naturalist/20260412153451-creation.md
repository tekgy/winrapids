# naturalist-corner-ownership-table

Created: 2026-04-12T15:34:51-05:00
By: naturalist

## Initial Notes

Concrete doc follow-up to the corners observation: build a table mapping every IEEE 754 corner case to the invariant or campsite that owns it. Rows: NaN propagation (I11), signed zero propagation (campsite naturalist-signed-zero-audit, possibly I12 someday), subnormal preservation (ESC-001 + P3 device precondition), FMA contraction (I3), reduction order (I5), rounding mode (I10/P2), special-value identity (e.g. exp(-inf)=+0, log(1)=+0) (campsite per function's oracle profile under the three-section schema), infinite arithmetic (inf + inf = inf, inf - inf = NaN, etc.) (P3 precondition), overflow/underflow flags (currently unaddressed — possible future campsite). Columns: corner case / invariant name / enforcement mechanism / known gaps. A 2-page doc would make the 'we own every corner' framing concrete and testable — you can audit it row by row. Naturalist could draft the skeleton; navigator and aristotle could review.
