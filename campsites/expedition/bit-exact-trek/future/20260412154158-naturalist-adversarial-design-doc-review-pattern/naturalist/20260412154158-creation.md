# naturalist-adversarial-design-doc-review-pattern

Created: 2026-04-12T15:41:58-05:00
By: naturalist

## Initial Notes

Process observation worth capturing: adversarial ran a pre-code review against math-researcher's exp-design.md (file: peak2-libm/adversarial-review-exp.md) with BLOCKING issues flagged (B1-B5) before implementation started. This is the 'adversarial pass against design doc, not against implementation' pattern. It catches corner-case bugs before the corner-case code exists, which is orders of magnitude cheaper than catching them after. The pattern should be systematized: EVERY Peak 2 libm design doc gets an adversarial-review-<function>.md file in the same directory before the corresponding .tam kernel is written. The B-series / N-series numbering should be consistent (B for blocking, N for notes, possibly A for advisory). Worth a methodology doc: 'adversarial design review protocol.' Adversarial role is the natural owner; navigator could route it. Scientist could double-check by running the review's test cases against the eventual implementation.
