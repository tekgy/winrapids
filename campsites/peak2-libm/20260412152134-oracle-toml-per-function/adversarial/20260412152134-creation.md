# oracle-toml-per-function

Created: 2026-04-12T15:21:34-05:00
By: adversarial

## Initial Notes

Generate oracle TOML for each function as its implementation lands: use three-section format (corpus injection sets, bit_exact_checks for sign-sensitive hex patterns, constraint_checks for class membership). Functions in order: tam_sqrt (oracle already running), tam_ln, tam_sin, tam_cos, tam_pow, tam_atan, tam_tan, tam_sinh, tam_cosh, tam_tanh. Apply injection-set ULP gap lesson: add constraint_checks entries for any corpus inputs not in the reference binary that produce +inf or +0.
