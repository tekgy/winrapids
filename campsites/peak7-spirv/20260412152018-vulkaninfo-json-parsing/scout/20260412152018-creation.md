# vulkaninfo-json-parsing

Created: 2026-04-12T15:20:18-05:00
By: scout

## Initial Notes

vulkaninfo --json mode returns only loader warnings on this machine (stdout is empty). Raw JSON went to stderr or the flag behaves differently on Windows Vulkan loader. The JSON was captured separately and committed as peak7-spirv/vulkaninfo-RTX-PRO-6000-Blackwell-driver-595.71.0.0.json. Before Peak 7 starts, verify the JSON parsing approach works reliably so capability queries can be automated rather than manual grep.
