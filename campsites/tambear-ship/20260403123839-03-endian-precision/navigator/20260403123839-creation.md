# 03-endian-precision

Created: 2026-04-03T12:38:39-05:00
By: navigator

## Initial Notes

Little-endian in .tam format. Default fp32 everywhere. fp64 when proof system flags condition number > threshold. .lock(precision=fp32) forces fp32. IEEE 754 deterministic merge order. Same bits every platform.
