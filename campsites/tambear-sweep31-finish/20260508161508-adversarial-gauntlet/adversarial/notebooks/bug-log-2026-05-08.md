# Adversarial Gauntlet — Bug Log (continued, post-compaction)

Session: tambear-sweep31-finish / 2026-05-08 (resumed after context compaction)
Gauntlet file: `R:\tambear\crates\tambear\tests\big_float_adversarial_gauntlet.rs`
Final state: **25 passing, 5 ignored** (attacks 15, 17, 18, 22, 23 are bug witnesses)

---

## Bugs found this resumed session

### attack21 (newly added, passes)
`a * 2 == a + a` for multi-limb witnesses — no bug. Regression witness.

### attack22 — NEW BUG (OPEN)
**`canonicalize_and_round` discards round/sticky when mag=0**

- File: `R:\tambear\crates\tambear\src\primitives\big_float\arith.rs` ~line 884
- Trigger: subtraction where `cmp_limbs(large_aligned, small_aligned) == Equal` but
  `round_bit | sticky_bit != 0`. The `cmp_with_hidden` redirect sends this to the
  `Less` arm. `diff = sub(small_aligned, large_aligned) = 0`. Then
  `canonicalize_and_round(zeros, ..., round_bit=1, sticky_bit=0, ...)` is called.
  `top_bit_position` returns `None` → fallback returns zero, discarding round/sticky.
- Wrong under RTN and RTAZ. Zero is the correct result under RNE, RTZ, RTP.
- Concrete construction:
  - `a = +1.0` at p=107: `BigFloat::from_raw_limbs(false, 0, 107, vec![0, 1u64<<42])`
  - `b = -(1.0 + 2^{-107})` at p=200: `BigFloat::from_raw_limbs(true, 0, 200, vec![0, 1u64<<28, 0, 1u64<<7])`
  - `a.add(&b, RTN)` returns zero instead of `-(2^{-107})`.
- Test: `attack22_canonicalize_zero_mag_nonzero_round_sticky_rtn` (FAILING when run)

### attack23 — NEW BUG (OPEN, blocked by attack18)
**Newton reciprocal scaled-seed: extreme b.exponent panics in `to_f64()` before reaching exp_shift**

- File: `R:\tambear\crates\tambear\src\primitives\big_float\conversions.rs` line 345
- Same root as attack18 (to_f64 overflow). `newton_reciprocal` calls `b.to_f64()`.
  For `b.exponent = i64::MAX - 50`, panics in debug mode at `unbiased + F64_EXP_BIAS`.
- After fixing attack18 (saturating_add), attack23 will need re-evaluation to check
  whether `recip.exponent -= exp_shift` at arith.rs ~1314 then overflows i64.
- Test: `attack23_newton_seed_exponent_overflow_extreme_b` (FAILING when run)

---

## Previously found bugs (carried forward)

### attack15 + attack17 — div() drops NaN payload
- File: `R:\tambear\crates\tambear\src\primitives\big_float\arith.rs` ~line 320-322
- Fix: pattern-match on `self.kind` and `rhs.kind` to preserve payload, same as add/mul.

### attack18 — to_f64() overflows i64 for extreme exponents
- File: `R:\tambear\crates\tambear\src\primitives\big_float\conversions.rs` line 345
- Fix: `let biased_exp = unbiased.saturating_add(F64_EXP_BIAS as i64);`
- Blocks attack23.

---

## Convergence — systemic patterns

Three independent patterns:
1. **Pattern A** (attacks 15, 17): div() missing payload-preserve. One-function fix.
2. **Pattern B** (attacks 18, 23): to_f64() i64 overflow at extreme exponents. One-line fix (saturating_add) removes both. Highest blast radius — affects all callers of to_f64().
3. **Pattern C** (attack 22): canonicalize_and_round silently drops sub-ULP contribution when mag=0. Latent TODO in code comment. Requires adding a "sub-ULP rounding" code path.

Fix priority: Pattern B first (blocks attack23 analysis), then Pattern A (straightforward), then Pattern C (most surgical).
