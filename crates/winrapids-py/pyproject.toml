[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "winrapids-core"
version = "0.1.0"
description = "WinRapids native core — PyO3 bindings for the pipeline compiler"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
