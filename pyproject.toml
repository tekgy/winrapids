[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "winrapids"
version = "0.1.0"
description = "Windows-native GPU data science toolkit"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
